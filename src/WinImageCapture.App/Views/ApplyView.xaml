<UserControl x:Class="WinImageCapture.App.Views.ApplyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Source .wim file" VerticalAlignment="Center" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SourceWimPath, UpdateSourceTrigger=PropertyChanged}" Margin="4" />
        <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Command="{Binding BrowseSourceCommand}" Margin="4" Padding="8,2" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Image index" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ImageIndex, UpdateSourceTrigger=PropertyChanged}" Margin="4" HorizontalAlignment="Left" Width="80" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Target folder/drive" VerticalAlignment="Center" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TargetDirectory, UpdateSourceTrigger=PropertyChanged}" Margin="4" />
        <Button Grid.Row="2" Grid.Column="2" Content="Browse..." Command="{Binding BrowseTargetCommand}" Margin="4" Padding="8,2" />

        <Button Grid.Row="3" Grid.Column="1" Content="Apply Image" Command="{Binding ApplyCommand}" HorizontalAlignment="Left" Padding="16,6" Margin="4,12,4,4" />

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Vertical" Margin="4">
            <ProgressBar Height="20" Minimum="0" Maximum="100" Value="{Binding PercentComplete, Mode=OneWay, TargetNullValue=0}" />
            <TextBlock Text="{Binding StatusMessage}" Margin="0,4,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
