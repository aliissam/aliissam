<UserControl x:Class="WinImageCapture.App.Views.CaptureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Source folder/drive" VerticalAlignment="Center" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SourceDirectory, UpdateSourceTrigger=PropertyChanged}" Margin="4" />
        <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Command="{Binding BrowseSourceCommand}" Margin="4" Padding="8,2" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Destination .wim file" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DestinationWimPath, UpdateSourceTrigger=PropertyChanged}" Margin="4" />
        <Button Grid.Row="1" Grid.Column="2" Content="Browse..." Command="{Binding BrowseDestinationCommand}" Margin="4" Padding="8,2" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Image name" VerticalAlignment="Center" />
        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding ImageName, UpdateSourceTrigger=PropertyChanged}" Margin="4" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Description" VerticalAlignment="Center" />
        <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding ImageDescription, UpdateSourceTrigger=PropertyChanged}" Margin="4" />

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Compression" VerticalAlignment="Center" />
        <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding CompressionLevels}" SelectedItem="{Binding Compression}" Margin="4" HorizontalAlignment="Left" Width="160" />

        <CheckBox Grid.Row="5" Grid.Column="1" Content="Verify integrity after capture" IsChecked="{Binding CheckIntegrity}" Margin="4" />

        <Button Grid.Row="6" Grid.Column="1" Content="Capture Image" Command="{Binding CaptureCommand}" HorizontalAlignment="Left" Padding="16,6" Margin="4,12,4,4" />

        <StackPanel Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Vertical" Margin="4">
            <ProgressBar Height="20" Minimum="0" Maximum="100" Value="{Binding PercentComplete, Mode=OneWay, TargetNullValue=0}" />
            <TextBlock Text="{Binding StatusMessage}" Margin="0,4,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
